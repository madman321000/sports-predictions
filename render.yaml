services:
  - type: web
    name: sports-predictions-api
    runtime: docker
    plan: free
    autoDeployTrigger: off
    healthCheckPath: /api/health
    envVars:
      - key: FORECAST_MODEL_DIR
        value: /etc/secrets
      - key: FRONTEND_ORIGIN
        sync: false
